-- Per-scope pull cursor (architecture §5.4).
CREATE TABLE sync_state (
    scope TEXT NOT NULL PRIMARY KEY,       -- 'brew_logs'
    cursor TEXT,                           -- OPAQUE string; never compared numerically client-side
    last_synced_at INTEGER
);
